# 开发环境

# spring相关配置
spring:
    # 数据源配置
    datasource:
        url: jdbc:mysql://localhost:3306/transformer?zeroDateTimeBehavior=convertToNull&relaxAutoCommit=true&useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&allowMultiQueries=true
        username: root
        password: lizhiyong
        driver-class-name: com.mysql.jdbc.Driver

# 服务器
server:
    #端口
    port: 9301
